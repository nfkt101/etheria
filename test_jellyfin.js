const fs = require('fs');
const token = 'placeholder'; // Will need to get it from localstorage, wait I can't read localstorage easily from node. 
// BUT Jellyfin is running on localhost:8096 without auth for local network or I can just use curl.
